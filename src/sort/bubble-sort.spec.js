import { bubbleSort } from "./bubble-sort";

describe("bubbleSort", () => {
  it("returns the same value", () => {
    // Arrange
    const x = "Hello Jasmine!";

    // Assert
    expect(x).toBe("Hello Jasmine!");
  });
});
